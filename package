cat > package.json << 'EOF'
{
  "name": "leaseloop-web",
  "version": "1.0.0",
  "description": "Student housing web app",
  "main": "app.js",
  "scripts": {
    "start": "python -m http.server 3000"
  }
}
EOF